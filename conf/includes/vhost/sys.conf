<LocationMatch "/sys">
  <IfModule mod_authz_host.c>
    Require local
  </IfModule>

  <IfModule !mod_authz_host.c>
    Order Deny,Allow
    Deny from all
    Allow from 127.0.0.1
  </IfModule>
</LocationMatch>

