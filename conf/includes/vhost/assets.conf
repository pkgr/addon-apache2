<Directory "/opt/_APP_NAME_/public">
  # required on apache 2.4+
  Require all granted
  Order deny,allow
  Allow from all
</Directory>

<Location /assets/>
  ExpiresActive On ExpiresDefault "access plus 1 year"
  ProxyPass !
</Location>
